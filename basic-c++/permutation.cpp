#include <iostream>

using namespace std;

int main()
{
    vector<int> a = {1, 2, 3};

    sort(v.begin(), v.end());
    do
    {
        for (int i : a)
            cout << i << " ";
        cout << endl;
    } while (next_permutation(a.begin(), a.end()));
}
